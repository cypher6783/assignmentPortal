<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment Portal</title>
    <!-- Use Inter font from Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header">
        <div class="container header-content">
            <a href="/" class="logo">AssignmentPortal</a>
            <nav class="nav">
                <% if (typeof user !== 'undefined' && user) { %>
                    <span class="text-muted text-sm">Welcome, <strong><%= user.username || user.name %></strong></span>
                    <% if (user.role === 'student') { %>
                        <a href="/auth/change-password" class="nav-link">Password</a>
                    <% } %>
                    <a href="/auth/logout" class="nav-link" style="color: var(--danger);">Logout</a>
                <% } %>
            </nav>
        </div>
    </header>

    <main class="container">
